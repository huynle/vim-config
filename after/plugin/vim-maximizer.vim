if !get(g:, 'loaded_vim_maximizer', v:false)
  finish
endif

" needs to be set before loading of plugins.. at this point is too late
" let g:maximizer_default_mapping_key = '<c-w>z'
" let g:maximizer_set_mapping_with_bang = 1

